saldo = 1000
limite = 500

print(saldo is limite)      # Verifica se os dois apontam para o mesmo objeto na memória
print(saldo is not limite)  # Verifica se os dois não apontam para o mesmo objeto na memória    
